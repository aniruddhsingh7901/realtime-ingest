"""Central config: reads .env and exposes settings.

To be implemented in Step 3. Will hold:
- Macrocosmos API base URL + auth token
- Miner hotkey ss58 + coldkey wallet name
- Path to proxies.txt / accounts.db
- Cache TTLs, worker count, retry limits
- Log level
"""
