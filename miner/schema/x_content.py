"""XContent pydantic model — MUST match sn13 exactly.

To be implemented in Step 4. Source of truth:
    data-universe/scraping/x/model.py  (XContent class)

Any field mismatch → validators' spot-check re-scrape fails
→ credibility drops → zero rewards.
"""
